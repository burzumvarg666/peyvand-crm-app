# وضعیت آماده‌سازی نسخهٔ آنلاین

مخزن مقصد: https://github.com/burzumvarg666/peyvand-crm-app

هدف تأییدشده: اجرای CRM در مرورگر با داده‌های مشترک و انتشار تغییرات از GitHub روی Vercel.

نسخهٔ ۰.۵.۲ شامل جداکنندهٔ سه‌رقمی مبلغ فرصت فروش، نمایش شمسی موعد و وضعیت سررسید است. تغییرات ردیف و مرتب‌سازی نسخهٔ ۰.۵.۱ نیز حفظ شده‌اند.

فایل .github/workflows/web-check.yml بررسی TypeScript، آزمون‌های CRM و ساخت نسخهٔ وب را پس از انتقال به GitHub اجرا می‌کند. موفقیت این بررسی به معنی تکمیل backend آنلاین نیست.

کارهای لازم پیش از استفادهٔ عملیاتی:

- اتصال مجاز GitHub و انتقال سورس به مخزن مقصد؛ هنوز انجام نشده است.
- اتصال پروژهٔ Vercel به این مخزن؛ هنوز انجام نشده است.
- تهیه یا اتصال پایگاه دادهٔ پایدار و تنظیم SUPABASE_URL و SUPABASE_ANON_KEY در محیط Vercel، بدون قرار دادن مقادیر محرمانه در سورس.
- تکمیل backend و مهاجرت‌های دیتابیس برای محصولات، پیش‌فاکتورها، انبار و اتوماسیون؛ طرح فعلی database/001_crm.sql را معادل قابلیت‌های کامل دسکتاپ فرض نکنید.
- آزمون ورود کاربران، جداسازی سازمان‌ها و نقش‌ها، ثبت هم‌زمان موجودی و جلوگیری از تکرار اتوماسیون.
- انتقال اختیاری داده‌های آفلاین با پشتیبان‌گیری و اعتبارسنجی؛ نصب یا انتشار وب به‌خودی‌خود داده‌های SQLite را منتقل نمی‌کند.

خروجی portable فعلی همچنان آفلاین است. این آماده‌سازی، اتصال آنلاین تکمیل‌شده یا همگام‌سازی داده را ادعا نمی‌کند.
