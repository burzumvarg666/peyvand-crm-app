# Peyvand CRM 0.6.0

مبنا: سورس online-recovery-brand در commit 048602f8aef3e4ef6c5ae5ab879660742721e28d، به‌علاوه اصلاحات آزموده‌شدهٔ بستهٔ آفلاین UI-Fixed-v2. سورس قبلی حذف نشده است.

امکانات جدید:
- پشتیبان روزانه هنگام بازبودن برنامه و پیش از ذخیره، حذف، عملیات انبار، ادغام و درون‌ریزی؛ بازکردن پوشهٔ پشتیبان از تنظیمات. پشتیبان‌های قبلی خودکار حذف نمی‌شوند.
- فیلتر شهر و بازهٔ تاریخ ثبت، پاک‌کردن فیلترها و ذخیرهٔ فیلتر دلخواه؛ ذخیرهٔ فیلترهای ویندوز در پایگاه دادهٔ محلی.
- انتخاب مستقیم سال و ماه در تقویم شمسی.
- هشدار نام، ایمیل و تلفن مشابه پیش از ذخیره؛ بررسی و ادغام اختیاری در نسخهٔ آفلاین. رکورد اصلی انتخاب می‌شود، ارتباط‌ها منتقل و تصویر کامل رکورد دوم در یادداشت حفظ می‌شود.
- پیگیری امروز، عقب‌افتاده، بدون تاریخ و مشتریان بدون فعالیت تکمیل‌شده در ۳۰ روز اخیر.
- درون‌ریزی افزایشی پشتیبان JSON نسخهٔ آفلاین از تنظیمات؛ رکوردهای دارای شناسهٔ موجود وارد نمی‌شوند. فایل اصلی واردشده و پشتیبان قبل از ورود نگهداری می‌شوند.
- مدیریت کاربران آنلاین برای مالک: تعلیق، فعال‌سازی و پایان اعتبار؛ کنترل دسترسی در پایگاه داده. نسخهٔ کاملاً آفلاین از این پنل کنترل نمی‌شود.

اعتبارسنجی:
- ۱۴ آزمون آفلاین، ۷ آزمون CRM و ۶ آزمون امکانات جدید موفق.
- جریان مرورگر → API محلی → SQLite → بازخوانی برای تبدیل سرنخ، فیلتر، هشدار مشابه و ادغام آزموده شد.
- پنجرهٔ حساب در عرض‌های ۱۲۸۰، ۹۰۰، ۷۱۰ و ۴۸۰ بدون سرریز افقی بررسی شد.
- آزمون SQL با کاربر موقت و rollback: منع مدیریت برای کاربر عادی، تعلیق، تاریخ انقضا، فعال‌سازی و RLS موفق. دادهٔ آزمایشی باقی نماند.
- ساخت Next.js و TypeScript موفق.
- اجرای فایل EXE در خود ویندوز در این محیط انجام نشده است.
- ورود واقعی با رمز، دریافت ایمیل تأیید و مسیر کامل ورود مالک در این نوبت آزموده نشده‌اند.
- آزمون قدیمی HTTP مربوط به حالت بدون اتصال، موفق اعلام نمی‌شود.

روش اجرای بسته:
۱. برنامهٔ قبلی را ببندید.
۲. ZIP را کامل در یک پوشهٔ جدید استخراج کنید؛ پوشهٔ قبلی را نگه دارید.
۳. Peyvand CRM.exe را اجرا کنید؛ فایل EXE را به‌تنهایی از پوشه جدا نکنید.
۴. داده‌ها در همان مسیر قبلی %APPDATA%\Peyvand CRM\peyvand.sqlite خوانده می‌شوند. بسته هیچ دادهٔ مشتری همراه ندارد.

پشتیبان‌های خودکار در همان رایانه‌اند؛ برای حفظ اطلاعات در خرابی دیسک، از تنظیمات یک نسخه در محل دیگری ذخیره کنید.

وضعیت انتشار این تحویل: سورس در شاخه feature/peyvand-060-workflows و commit b9084da3bd13376fee5a3316b5060a7eb55d1545 ثبت شد. کنترل دسترسی پایگاه داده اعمال و آزموده شد. پنل جدید روی Vercel منتشر نشده است؛ ابزار انتشار اتصال فعلی خطای Tool not found داد.
